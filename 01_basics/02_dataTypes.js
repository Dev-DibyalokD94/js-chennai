"use strict"; //treat all JS code as newer version, the engine itself will treat the code for newer version
/*
but now a days, you don't even need to write this above line, as all runs in strict mode only
*/ 

// alert("hello") we are using node js, not browser, in browser the output will be displayed.


let name = "DD" //string datatype
let age = 20 //number
let isLoggedIn = false // boolean

//premitive datatypes:

//number => 2 to the power of 52 or 53
//bigint 
//String => use double quotes ""
//boolean -> true/false
//null -> this is a standalone value, it's a special type, it's a representation of empty value
//undefined -> we have not assigned value
//symbol -> for unique 

// --->

//object

console.log(typeof undefined);


